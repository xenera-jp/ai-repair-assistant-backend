<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aifieldservice.repairassistant.dao.retrieval.ManualKnowledgeProjectionMapper">
    <sql id="Columns">id, document_name, model, problem_type_code, knowledge_type, error_code, title, title_ja, summary, summary_ja, source_quote, source_anchor, source_region_json, action_steps_json, action_steps_ja_json, safety_warnings_json, safety_warnings_ja_json, candidate_codes_json, source_reference, pdf_page_index, printed_page_label, section_path, trust_level</sql>
    <select id="findStructuredManuals" resultType="com.aifieldservice.repairassistant.domain.retrieval.model.ManualKnowledgeProjection">
        SELECT <include refid="Columns"/> FROM manual_knowledge_projection_v1
        WHERE model = #{model} AND problem_type_code = #{problemTypeCode}
          AND (#{errorCode} = '' OR error_code = #{errorCode} OR error_code IS NULL OR error_code = '')
        ORDER BY CASE WHEN #{errorCode} &lt;&gt; '' AND error_code = #{errorCode} THEN 0 WHEN error_code IS NULL OR error_code = '' THEN 1 ELSE 2 END,
                 CASE knowledge_type WHEN 'FAULT_DEFINITION' THEN 0 ELSE 1 END,
                 pdf_page_index, id
        LIMIT 6
    </select>
    <select id="findByIds" resultType="com.aifieldservice.repairassistant.domain.retrieval.model.ManualKnowledgeProjection">
        SELECT <include refid="Columns"/> FROM manual_knowledge_projection_v1
        WHERE id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>
</mapper>

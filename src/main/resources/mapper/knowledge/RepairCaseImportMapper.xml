<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aifieldservice.repairassistant.dao.knowledge.RepairCaseImportMapper">
 <select id="countAll" resultType="int">SELECT COUNT(*) FROM repair_case_projection_v1</select>
 <insert id="insertIgnoreVersion">INSERT IGNORE INTO knowledge_unit_version (knowledge_unit_id,version_no,schema_version,title,language_code,trust_level,status,content_json,source_fingerprint,content_fingerprint,qdrant_point_id,published_at) VALUES (#{unitId},1,'REPAIR_CASE_V1',#{title},'ja-JP',#{trustLevel},'PUBLISHED',CAST(#{contentJson} AS JSON),#{sourceFingerprint},#{contentFingerprint},#{pointId},CURRENT_TIMESTAMP(3))</insert>
 <select id="findVersionId" resultType="long">SELECT id FROM knowledge_unit_version WHERE knowledge_unit_id=#{unitId} AND version_no=1</select>
 <insert id="linkProblemType">INSERT IGNORE INTO knowledge_unit_problem_type (knowledge_unit_version_id,problem_type_id,rank_no) VALUES (#{versionId},#{problemTypeId},1)</insert>
 <insert id="insertSearchProjection">INSERT IGNORE INTO knowledge_search_projection (knowledge_unit_version_id,projection_type,projection_text,projection_hash) VALUES (#{versionId},#{type},#{text},#{hash})</insert>
 <insert id="upsertProjection">INSERT INTO repair_case_projection_v1 (knowledge_unit_version_id,reception_id,model,serial_number,customer_site_name,received_at,problem_type_code,problem_type_label,error_codes_json,complaint,onsite_observation,cause_text,action_text,final_resolved,first_fix,visit_count,total_duration_minutes,parts_json,problem_projection,resolution_projection,source_reference,qdrant_point_id,trust_level) VALUES (#{versionId},#{receptionId},#{model},#{serialNumber},#{customerSiteName},#{receivedAt},#{problemTypeCode},#{problemTypeLabel},CAST(#{errorCodesJson} AS JSON),#{complaint},#{onsiteObservation},#{causeText},#{actionText},#{finalResolved},#{firstFix},#{visitCount},#{totalDurationMinutes},CAST(#{partsJson} AS JSON),#{problemProjection},#{resolutionProjection},#{sourceReference},#{pointId},#{trustLevel}) ON DUPLICATE KEY UPDATE problem_type_code=VALUES(problem_type_code),problem_type_label=VALUES(problem_type_label),error_codes_json=VALUES(error_codes_json),complaint=VALUES(complaint),onsite_observation=VALUES(onsite_observation),cause_text=VALUES(cause_text),action_text=VALUES(action_text),final_resolved=VALUES(final_resolved),first_fix=VALUES(first_fix),visit_count=VALUES(visit_count),total_duration_minutes=VALUES(total_duration_minutes),parts_json=VALUES(parts_json),problem_projection=VALUES(problem_projection),resolution_projection=VALUES(resolution_projection),source_reference=VALUES(source_reference),trust_level=VALUES(trust_level)</insert>
 <select id="findPendingVectors" resultType="com.aifieldservice.repairassistant.domain.knowledge.model.PendingCaseVector">SELECT id,reception_id,model,problem_type_code,problem_projection,qdrant_point_id FROM repair_case_projection_v1 WHERE indexed=FALSE ORDER BY id</select>
 <update id="markIndexed">UPDATE repair_case_projection_v1 SET indexed=TRUE WHERE indexed=FALSE AND id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach></update>
</mapper>

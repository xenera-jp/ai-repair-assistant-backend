<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aifieldservice.repairassistant.dao.knowledge.KnowledgeBaseMapper">
    <insert id="upsertActive">
        INSERT INTO knowledge_base (code, name, status)
        VALUES (#{code}, #{name}, 'ACTIVE')
        ON DUPLICATE KEY UPDATE name = VALUES(name), status = 'ACTIVE'
    </insert>
    <select id="findIdByCode" resultType="long">
        SELECT id FROM knowledge_base WHERE code = #{code}
    </select>
</mapper>

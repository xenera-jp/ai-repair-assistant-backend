<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aifieldservice.repairassistant.dao.knowledge.KnowledgeUnitMapper">
 <insert id="upsert">INSERT INTO knowledge_unit (knowledge_base_id,unit_key,unit_type,current_version_no) VALUES (#{knowledgeBaseId},#{unitKey},#{unitType},1) ON DUPLICATE KEY UPDATE current_version_no=1</insert>
 <select id="findId" resultType="long">SELECT id FROM knowledge_unit WHERE knowledge_base_id=#{knowledgeBaseId} AND unit_key=#{unitKey}</select>
</mapper>
